{
  "name": "danieladebisi/jesslovesinterior",
  "description": "Jes Love's Interior Design Studio - Professional interior design services in Groton, CT",
  "type": "project",
  "license": "MIT",
  "authors": [
    {
      "name": "Jes Love's Interior",
      "email": "info@jeslovesinterior.com"
    }
  ],
  "require": {
    "php": ">=7.4"
  },
  "require-dev": {},
  "autoload": {},
  "scripts": {
    "post-install-cmd": [
      "npm install",
      "npm run build",
      "mkdir -p public_html",
      "cp -r out/* public_html/ 2>/dev/null || cp -r out/* public_html/"
    ],
    "post-update-cmd": [
      "npm install",
      "npm run build", 
      "mkdir -p public_html",
      "cp -r out/* public_html/ 2>/dev/null || cp -r out/* public_html/"
    ]
  },
  "config": {
    "optimize-autoloader": true,
    "preferred-install": "dist",
    "sort-packages": true
  },
  "minimum-stability": "stable",
  "prefer-stable": true
}